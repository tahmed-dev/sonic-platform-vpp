DEP_FILES   := $(SONIC_COMMON_FILES_LIST) $(PLATFORM_PATH)/platform-modules-vpp.mk $(PLATFORM_PATH)/platform-modules-vpp.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(addprefix $(PLATFORM_PATH)/,$(shell cd $(PLATFORM_PATH) && git ls-files $(VPP_PLATFORM_MODULE_REL_PATH)))

$(VPP_PLATFORM_MODULE)_CACHE_MODE    := GIT_CONTENT_SHA
$(VPP_PLATFORM_MODULE)_DEP_FLAGS     := $(SONIC_COMMON_FLAGS_LIST)
$(VPP_PLATFORM_MODULE)_DEP_FILES     := $(DEP_FILES)